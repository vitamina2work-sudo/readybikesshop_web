/** When true, public catalog/settings never call Supabase. */
export const USE_LOCAL_MOCK = true

export const LOCAL_MODE_WRITE_ERROR =
  'Modo local activo: Supabase está desconectado (HTTP 402). Los datos se sirven desde src/data/staticData.ts.'
